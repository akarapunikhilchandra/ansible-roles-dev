- name: check roboshop user exist or not
  ansible.builtin.command: id roboshop
  register: out #output is variable name
  ignore_errors: true

- when: out.rc != 0
  name: create user roboshop
  become: yes # we need sudo access for this task only
  ansible.builtin.user:
    name: roboshop

- name: check /app directory exists or not
  ansible.builtin.stat:
    path: /app
  register: directory

- name: print the directory stats
  ansible.builtin.debug:
    msg: "output: {{directory}}"

- when: directory.stat.exists == False
  name: create /app directory
  ansible.builtin.file:
    path: /app
    state: directory

# - name: "download {{component}} artifact"
#   ansible.builtin.get_url:
#     url: "https://roboshop-builds.s3.amazonaws.com/{{component}}.zip"
#     dest: /tmp

# download artifact from nexus
- name: download artifact from nexus
  ansible.builtin.get_url:
    url: "http://54.175.37.198:8081/repository/catalogue/com/roboshop/catalogue/1.0.1/catalogue-1.0.1.zip"
    # url: "{{nexus_url}}/{{component}}/{{group_id}}/{{component}}/{{app_version}}/{{component}}-{{app_version}}.zip"
    dest: "/app/catalogue-1.0.1.zip"
    username: "{{nexus_username}}"
    password: "{{nexus_password}}"

- name: extract {{component}} artifact
  ansible.builtin.unarchive:
    src: "/app/catalogue-1.0.1.zip"
    dest: "/app"
    remote_src: yes